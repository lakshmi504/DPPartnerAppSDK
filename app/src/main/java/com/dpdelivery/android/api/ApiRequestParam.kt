package com.dpdelivery.android.api

import com.dpdelivery.android.utils.SharedPreferenceManager
import com.google.gson.JsonObject

class ApiRequestParam(var appSharedPrefs: SharedPreferenceManager) {

    var respParamObj = JsonObject()
}